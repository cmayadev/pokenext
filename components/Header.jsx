import styles from '../styles/header.module.css'

const Header = () => {
    return ( 
        <>
            <nav className={styles.header}>
                Pokedex
            </nav>
        </>
    );
}

export default Header;